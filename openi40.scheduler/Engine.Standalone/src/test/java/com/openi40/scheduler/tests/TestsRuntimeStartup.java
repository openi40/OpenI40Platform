/**
 * 
 * This code is part of the OpenI40 open source advanced production scheduler
 * platform suite, have look to its licencing options.
 * Web site: http://openi40.org/  
 * Github: https://github.com/openi40/OpenI40Platform
 * We hope you enjoy implementing new amazing projects with it.
 * @author architectures@openi40.org
 *
 */
package com.openi40.scheduler.tests;

public class TestsRuntimeStartup
{
	private static boolean initialized = false;
	public static void Initialize()
	{
		/*
		EngineRuntime.Initialize();
		EngineRuntime.getRuntime().getSetupEvaluatorFactoryRepository().addFactory(new TestingSetupEvaluatorFactory());
		EngineRuntime.getRuntime().getCapacityEvaluationFactoryRepository().addFactory(new TestCapacityEvaluatorFactory());
		//EngineRuntime.Runtime.CalendarManagerFactoryRepository.AddFactory(new FakeCalendarManager.FakeCalendarManagerFactory());
		EngineRuntime.getRuntime().getCalendarManagerFactoryRepository().addFactory(new EasyCalendarManager.EasyCalendarManagerFactory());
		//EngineRuntime.getRuntime().getScheduleDataHolderRuntimeFactoryRepository().AddFactory(new TestScheduleDataHolderRuntime.TestScheduleDataHolderRuntimeFactory());
		initialized = true;
		*/
	}
}