<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>com.openi40.scheduler</groupId>
		<artifactId>parent</artifactId>
		<version>00.41.BETA-SNAPSHOT</version>
		
	</parent>
	<artifactId>Client.RestApi</artifactId>
	<dependencies>

		<dependency>
			<groupId>com.openi40.scheduler</groupId>
			<artifactId>Engine</artifactId>
			<version>${project.version}</version>
		</dependency>

		<dependency>
			<groupId>com.openi40.scheduler</groupId>
			<artifactId>ApsDataCache</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>com.openi40.scheduler</groupId>
			<artifactId>Client.Model</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>com.openi40.commons</groupId>
			<artifactId>Mapper</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>com.openi40.scheduler</groupId>
			<artifactId>ApsModel2ClientModelMappers</artifactId>
			<version>${project.version}</version>
		</dependency>
	</dependencies>
	<!-- 
	<build>
		<plugins>
			<plugin>
				<groupId>com.github.kongchen</groupId>
				<artifactId>swagger-maven-plugin</artifactId>
				<version>${swagger-maven-plugin-version}</version>
				<configuration>
					<apiSources>
						<apiSource>
							<info>
								<title>openi40.scheduler API</title>
								<version>${project.version}</version>
							</info>
							<locations>
								<location>com.openi40.scheduler.client.bryntum.restapi</location>
								<location>com.openi40.scheduler.client.restapi</location>
							</locations>
							<swaggerDirectory>${basedir}/src/main/resources/GenSwagger</swaggerDirectory>
							<outputPath>${basedir}/src/main/resources/GenSwagger</outputPath>
							<swaggerFileName>openi40.scheduler.Client.RestApi</swaggerFileName>
							
							<outputFormats>json,yaml</outputFormats>
							<springmvc>true</springmvc>
						</apiSource>
					</apiSources>


				</configuration>
				<executions>
					<execution>
						<phase>compile</phase>
						<goals>
							<goal>generate</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>  -->
</project>